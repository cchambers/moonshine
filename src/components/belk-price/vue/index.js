/* eslint-disable no-undef */
import Vue from 'vue';
import BelkPrice from './BelkPrice.vue';

Vue.customElement('belk-price', BelkPrice);

/*
* Unit tests:
* https: //vue-test-utils.vuejs.org/guides/getting-started.html
*/
// describe('BelkPrice', () => {
//   // Inspect the raw component options
//   it('has a `snap` method', () => {
//     expect(typeof BelkPrice.methods.snap).toBe('function');
//   });
// });
