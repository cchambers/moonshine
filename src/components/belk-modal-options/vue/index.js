/* eslint-disable no-undef */
import Vue from 'vue';
import BelkModalOptions from './BelkModalOptions.vue';

Vue.customElement('belk-modal-options', BelkModalOptions);

/*
* Unit tests:
* https: //vue-test-utils.vuejs.org/guides/getting-started.html
*/
// describe('BelkModalOptions', () => {
//   // Inspect the raw component options
//   it('has a `snap` method', () => {
//     expect(typeof BelkModalOptions.methods.snap).toBe('function');
//   });
// });
