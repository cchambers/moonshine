/* eslint-disable no-undef */
import Vue from 'vue';
import BelkBadge from './BelkBadge.vue';

Vue.customElement('belk-badge', BelkBadge);

/*
* Unit tests:
* https: //vue-test-utils.vuejs.org/guides/getting-started.html
*/
// describe('BelkBadge', () => {
//   // Inspect the raw component options
//   it('has a `snap` method', () => {
//     expect(typeof BelkBadge.methods.snap).toBe('function');
//   });
// });
